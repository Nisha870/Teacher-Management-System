# Copy this file to `.env` and fill in your real values.
# Then load it before running the app, e.g. with python-dotenv
# or by exporting the variables in your terminal:
#   export $(cat .env | xargs)   (macOS/Linux)

DB_HOST=localhost
DB_USER=root
DB_PASSWORD=your_mysql_password
DB_NAME=teacher_db
